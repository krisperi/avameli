# avameli
Used for organize and pass information

Need to install Python (Windows machine used)

Python libraries:
Netmiko --- (pip3 install netmiko)
Tkinter --- (pip3 install pytk)

Git commands used for several updates:

git status
git add .
git commit -m "A pretty little message"
git push

git pull (I used 3 different computers for this project)